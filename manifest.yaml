# 🌟 Vibe Code Landing Zone — manifest.yaml (Website Ready)
apiVersion: v1
kind: deployable-architecture
metadata:
  name: vibe-code-landing-zone
  version: 1.0.1
  displayName: Vibe Code Landing Zone
  description: Paste your vibe, exhale, and ship — a static site on IBM Cloud Object Storage (website enabled).
spec:
  deliveryMethod: terraform
  entrypoint: main.tf
  variables: variables.tf
  tags:
    - ibm-cloud
    - object-storage
    - static-website
    - website-ready
    - deployable-architecture
    - vibe-coding
  runtime:
    name: terraform
    version: "1.5"
