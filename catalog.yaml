id: vibe-coder
version: 0.1.0
type: deployable_architecture
displayName: Vibe Coder — Instant SPA
description: >
  Deploy a minimal "Vibe Coder" SPA. Paste your HTML and see it served instantly from IBM COS.

tags:
  - SPA
  - COS
  - Vibe Coding
  - IBM Cloud

deploy:
  terraform:
    main: main.tf
    inputs: inputs.tf

inputs:
  - id: vibe_code
    type: string
    label: Paste your Vibe Code (HTML)
    description: >
      Enter your HTML or full SPA content here. It will be served instantly from IBM Cloud Object Storage.
    required: true
  - id: ibmcloud_api_key
    type: password
    label: IBM Cloud API Key
    description: API key for Terraform to provision resources.
    required: true
