{
  "id": "vibe-coder",
  "version": "0.1.0",
  "displayName": "Vibe Coder — Instant SPA",
  "description": "Deploy an instant single-page app (SPA) on IBM Cloud Object Storage. Paste or upload your HTML and get a live app immediately. Don't worry, it comes with a default sample!",
  "type": "deployable_architecture",
  "deliveryMethod": "terraform",
  "icon": "https://example.com/vibe-coder-icon.png",
  "tags": [
    "vibe coding",
    "SPA",
    "IBM Cloud",
    "COS",
    "Terraform"
  ],
  "variables": [
    {
      "name": "region",
      "type": "string",
      "description": "IBM Cloud region to deploy your SPA. Don't worry, default is 'us-south'.",
      "default": "us-south",
      "ui_hints": {
        "group": "Setup",
        "label": "Region",
        "placeholder": "e.g. us-south"
      }
    },
    {
      "name": "cos_name",
      "type": "string",
      "description": "COS instance name to host your vibe-coded app. Don't worry, a default name is prefilled.",
      "default": "vibe-coder-cos",
      "ui_hints": {
        "group": "Setup",
        "label": "COS Instance Name",
        "placeholder": "vibe-coder-cos"
      }
    },
    {
      "name": "bucket_name",
      "type": "string",
      "description": "Bucket name where your SPA will live. Don't worry, a default bucket is prefilled.",
      "default": "vibe-coder-sample-bucket",
      "ui_hints": {
        "group": "Setup",
        "label": "Bucket Name",
        "placeholder": "vibe-coder-sample-bucket"
      }
    },
    {
      "name": "make_public",
      "type": "boolean",
      "description": "Whether the COS bucket should be public. Default is false.",
      "default": false,
      "ui_hints": {
        "group": "Access",
        "label": "Make Public"
      }
    },
    {
      "name": "sample_app_html",
      "type": "string",
      "description": "Optional HTML content for the default SPA. Leave empty to use preloaded sample.",
      "default": ""
    }
  ]
}
